apiVersion: apps/v1
kind: Deployment
metadata:
  name: movies-service
  namespace: eks-dev
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: movies-service
  template:
    metadata:
      labels:
        app: movies-service
    spec:
      containers:
        - image: vbuxe-dev
          name: movies-service
          ports:
            - containerPort: 8080
          env:
            - name: NATS_CLIENT_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            
            - name: JWT_KEY
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: JWT_KEY
            - name: SENDGRID
              valueFrom:
                secretKeyRef:
                  name: send-grid-secret
                  key: SENDGRID
            - name: TWILIO_ACCOUNT_SID
              valueFrom:
                secretKeyRef:
                  name: twilio-account-secret
                  key: TWILIO_ACCOUNT_SID
            - name: TWILIO_movies_TOKEN
              valueFrom:
                secretKeyRef:
                  name: twilio-movies-token-secret
                  key: TWILIO_movies_TOKEN
          readinessProbe:
            httpGet:
              path: api/movies/actuator/health
              port: 8080
            initialDelaySeconds: 30
          livenessProbe:
            httpGet:
              path: api/movies/actuator/health
              port: 8080
            initialDelaySeconds: 120
            periodSeconds: 20
            timeoutSeconds: 60
          resources:
            limits:
              cpu: 200m
              memory: 2Gi
            requests:
              cpu: 100m
              memory: 2Gi
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: movies-service-config
      imagePullSecrets:
        - name: movies-service-secret
        
        #
