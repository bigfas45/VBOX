apiVersion: v1
kind: Service
metadata:
  labels:
    name: movies-service
    namespace: eks-dev
  name: movies-service
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
  selector:
    name: movies-service
