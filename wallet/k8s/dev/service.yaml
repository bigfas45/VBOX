apiVersion: v1
kind: Service
metadata:
  labels:
    name: wallet-service
    namespace: eks-dev
  name: wallet-service
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
  selector:
    name: wallet-service
